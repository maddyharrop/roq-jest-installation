const favouriteFilms = ["2012", "Hanna", "Deadpool", "Daylight",]
console.log(favouriteFilms[0]);

favouriteFilms.push("Volcano");

console.table(favouriteFilms);

favouriteFilms.slice()

console.table(favouriteFilms);

favouriteFilms.unshift("Alive");